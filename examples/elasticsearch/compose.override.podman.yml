# Elasticsearch runs as uid 1000 (elasticsearch) inside the container.
# Map host UID to container uid 1000 so data files are owned by the host user.
services:
  elasticsearch:
    userns_mode: "keep-id:uid=1000,gid=1000"
